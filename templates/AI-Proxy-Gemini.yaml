name: AI-Proxy-Gemini
type: template
description: API template for AI-Proxy-Gemini
features:
  - cors
  - ai-auth
  - ai-proxy-googlecloud
parameters:
  - name: ModelBasePath
    paths:
      - $.endpoints[:1].basePath
    description: The base path to offer the AI proxy on.
    default: /gemini
    examples:
      - /gemini
      - /llm
  - name: AllowedModelPatterns
    displayName: AllowedModelPatterns
    description: Configuration input for AllowedModelPatterns
    default: gemini
    examples:
      - google
      - anthropic
    maps: {}
  - name: DeniedModelPatterns
    displayName: DeniedModelPatterns
    description: Configuration input for DeniedModelPatterns
    default: ""
    examples:
      - mythos
      - pro
    maps: {}
  - name: OverrideModel
    displayName: OverrideModel
    description: Configuration input for OverrideModel
    default: ""
    examples:
      - gemini-flash-latest
    maps: {}
  - name: DefaultModel
    displayName: DefaultModel
    description: Configuration input for DefaultModel
    default: ""
    examples:
      - gemini-flash-latest
    maps: {}
  - name: FailoverModel
    displayName: FallbackModel
    description: Configuration input for FallbackModel
    default: gemini-flash-latest
    examples:
      - gemini-flash-latest
    maps: {}
  - name: ServiceAccountAuth
    description: If a Google Cloud Service Account should be used in the proxy deployment, or not.
    default: true
    examples:
      - true
      - false
    maps:
      false: remove=$.targets[*].auth,$.targets[*].scopes,$.targets[*].httpTargetConnection.authentication
endpoints:
  - name: googlecloud
    basePath: /model
    routes:
      - name: default
        target: googlecloud
targets:
  - name: googlecloud
    url: https://aiplatform.googleapis.com
    auth: GoogleAccessToken
    scopes:
      - https://www.googleapis.com/auth/cloud-platform
